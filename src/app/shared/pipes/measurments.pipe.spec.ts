import { MeasurmentsPipe } from './measurments.pipe';

describe('MeasurmentsPipe', () => {
  it('create an instance', () => {
    const pipe = new MeasurmentsPipe();
    expect(pipe).toBeTruthy();
  });
});
